# MLOps Batch Signal Pipeline

This project implements a miniature MLOps-style batch pipeline that demonstrates reproducibility, structured metrics output, logging, and Docker-based deployment.

---

## Setup Instructions

Install dependencies locally:

```bash
pip install -r requirements.txt